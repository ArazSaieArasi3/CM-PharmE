# Evaluation Assertions

This directory is reserved for machine-readable expected assertions and test oracles used by B4/B5 evaluation.

Assertions added here should identify the competency question or validation rule they support, the relevant model version/commit, the expected result, and the evidence status. Historical publication claims should not be converted into executable assertions unless the repository contains enough information to reproduce them.