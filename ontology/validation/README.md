# Ontology Validation

B3 separates structural validation from full OWL DL reasoning. The canonical ontology is checked for syntax, referential integrity, serialization equivalence, stable identifiers, entity counts, curated semantic repairs, and SHACL structure. A full HermiT/ELK/ROBOT consistency result is not claimed until an exact reasoner and version are executed in CI/evaluation.
