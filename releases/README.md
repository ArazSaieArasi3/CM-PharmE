# CM-PharmE Releases

Each stable CM-PharmE release is treated as an immutable research snapshot. A release may contain the conceptual model, ontology distributions, catalogs, mappings, evaluation evidence, data samples/references, documentation metadata, and linked publications required to reproduce that state of the model.

The first materialized historical release is [`v1.0.0`](v1.0.0/manifest.yaml).
