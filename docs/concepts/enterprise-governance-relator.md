# Enterprise Governance Relator

- **Stable ID:** `CMPE-C0025`
- **Canonical stereotype:** `relator`
- **Domains:** `CMPE-D0001` (primary), `CMPE-D0004`
- **Incoming:** `CMPE-R0002`
- **Source occurrences:** 2

> **Review flag — stereotype conflict:** the v1.0 source model contains two graphical occurrences with this same label: one marked `relator` and another marked `mode`. The canonical concept registry retains one stable entity as `relator`; the conflict is preserved rather than silently discarded.

```xml
<owl:Class rdf:about="#Enterprise_Governance_Relator"/>
```
Formal resolution is deferred to the ontology-engineering cycle.
